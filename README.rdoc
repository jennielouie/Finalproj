Stitcherdoodle

Ideas:
Prevent user from adding duplicates of same project.  First attempted to do this through user_project model, validating for unique project id.  However, this prevent multiple users from using the same pattern.
